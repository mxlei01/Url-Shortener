# momoko_settings.py keeps all of the database access information

# dbname defines the database name that we are going to access
dbname = "url_db"

# user defines the user that is going to access postgres
user = "docker"

# password defines the password that we are going to use with the username to access postgres
password = "docker"

# host defines the url that the host is located
host = "localhost"

# port defines the port that we are going to connect to postgres
port = 5432

# num_connections defines minimum numbers of connections to maintain to postgres
num_connections = 1